console.log(document.querySelector('h1').textContent);

document.querySelector('h1').textContent = 'Hey, it\'s changed!!';

console.log(document.querySelector('.heading').textContent);


//project

<p> ? </p>
let secretNumber = Math.trunc(Math.random() * 6) + 1;

const x = 60;

document.querySelector('.check').addEventListener('click', function(){
    const inputUser =  Number(document.querySelector('.msg').value);
    console.log(inputUser, typeof inputUser);
    if(){
        document.querySelector('h1').textContent = 'Hey, it\'s changed!!';
        let dice = Math.trunc(Math.random() * 6) + 1;
    }

});

